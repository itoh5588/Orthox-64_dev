diff --git a/applets/usage_pod.c b/applets/usage_pod.c
index ccc166aed..9e6d3f0ee 100644
--- a/applets/usage_pod.c
+++ b/applets/usage_pod.c
@@ -71,7 +71,7 @@ int main(void)
 		} else {
 			printf(", ");
 		}
-		printf(usage_array[i].aname);
+		printf("%s", usage_array[i].aname);
 		col += len2;
 	}
 	printf("\n\n");
diff --git a/coreutils/test.c b/coreutils/test.c
index edc625f57..b406842e3 100644
--- a/coreutils/test.c
+++ b/coreutils/test.c
@@ -397,8 +397,16 @@ struct test_statics {
 	jmp_buf leaving;
 };
 
+#ifndef ORTHOX_BUSYBOX_TEST_PTR_HACK
+#define ORTHOX_BUSYBOX_TEST_PTR_HACK 0
+#endif
+
 /* See test_ptr_hack.c */
+#if ORTHOX_BUSYBOX_TEST_PTR_HACK
+extern struct test_statics *test_ptr_to_statics;
+#else
 extern struct test_statics *const test_ptr_to_statics;
+#endif
 
 #define S (*test_ptr_to_statics)
 #define args            (S.args         )
diff --git a/coreutils/test_ptr_hack.c b/coreutils/test_ptr_hack.c
index 5ba9dcc68..3f84f0209 100644
--- a/coreutils/test_ptr_hack.c
+++ b/coreutils/test_ptr_hack.c
@@ -7,7 +7,7 @@
 
 struct test_statics;
 
-#ifndef GCC_COMBINE
+#if !defined(GCC_COMBINE) || ORTHOX_BUSYBOX_TEST_PTR_HACK
 
 /* We cheat here. It is declared as const ptr in libbb.h,
  * but here we make it live in R/W memory */
diff --git a/include/libbb.h b/include/libbb.h
index 87f89c76d..b14178ec2 100644
--- a/include/libbb.h
+++ b/include/libbb.h
@@ -889,6 +889,11 @@ char *safe_gethostname(void) FAST_FUNC;
 /* Convert each alpha char in str to lower-case */
 char* str_tolower(char *str) FAST_FUNC;
 
+#ifdef __ORTHOS__
+#define utoa bb_utoa
+#define itoa bb_itoa
+#endif
+
 char *utoa(unsigned n) FAST_FUNC;
 char *itoa(int n) FAST_FUNC;
 /* Returns a pointer past the formatted number, does NOT null-terminate */
@@ -1862,7 +1867,23 @@ struct globals;
 /* '*const' ptr makes gcc optimize code much better.
  * Magic prevents ptr_to_globals from going into rodata.
  * If you want to assign a value, use SET_PTR_TO_GLOBALS(x) */
+/* Orthox: **Clang は const 宣言を見て読み出しを初期値 (NULL) に畳み込む。**
+ * SET_PTR_TO_GLOBALS で入れた値が見えなくなり、最初のストアが NULL+offset
+ * へのアクセスになって落ちる (aarch64 実機で awk が ESR=0x92000046 /
+ * FAR=0xc0 で必ず落ちていた)。実体 (libbb/ptr_to_globals.c) は元から
+ * 書き込み可能なので、宣言の const を外すだけでよい。
+ * shell/ash.c の ORTHOX_BUSYBOX_ASH_PTR_HACK、libbb/lineedit.c の
+ * ORTHOX_BUSYBOX_LINEEDIT_PTR_HACK、coreutils/test.c の
+ * ORTHOX_BUSYBOX_TEST_PTR_HACK と同じ手当てを、**共通のポインタにも**入れる
+ * —— awk など ptr_to_globals を使う applet が全部これに当たる */
+#ifndef ORTHOX_BUSYBOX_PTR_TO_GLOBALS_HACK
+#define ORTHOX_BUSYBOX_PTR_TO_GLOBALS_HACK 0
+#endif
+#if ORTHOX_BUSYBOX_PTR_TO_GLOBALS_HACK
+extern struct globals *ptr_to_globals;
+#else
 extern struct globals *const ptr_to_globals;
+#endif
 /* At least gcc 3.4.6 on mipsel system needs optimization barrier */
 #define barrier() __asm__ __volatile__("":::"memory")
 #define SET_PTR_TO_GLOBALS(x) do { \
diff --git a/libbb/lineedit.c b/libbb/lineedit.c
index 2a5d4e704..65501e5da 100644
--- a/libbb/lineedit.c
+++ b/libbb/lineedit.c
@@ -173,7 +173,19 @@ struct lineedit_statics {
 };
 
 /* See lineedit_ptr_hack.c */
+/* Orthox BusyBox build uses a writable pointer here to avoid Clang/RISC-V
+ * miscompiling the cast-based initialization hack used by upstream lineedit:
+ * with the `const` declaration Clang folds the load in read_line_input() to
+ * the initial NULL and the first store through S faults. Same fix as
+ * ORTHOX_BUSYBOX_ASH_PTR_HACK in shell/ash.c. */
+#ifndef ORTHOX_BUSYBOX_LINEEDIT_PTR_HACK
+#define ORTHOX_BUSYBOX_LINEEDIT_PTR_HACK 0
+#endif
+#if ORTHOX_BUSYBOX_LINEEDIT_PTR_HACK
+extern struct lineedit_statics *lineedit_ptr_to_statics;
+#else
 extern struct lineedit_statics *const lineedit_ptr_to_statics;
+#endif
 
 #define S (*lineedit_ptr_to_statics)
 #define state            (S.state           )
diff --git a/libbb/lineedit_ptr_hack.c b/libbb/lineedit_ptr_hack.c
index dc45855d5..cdc1ace0f 100644
--- a/libbb/lineedit_ptr_hack.c
+++ b/libbb/lineedit_ptr_hack.c
@@ -7,7 +7,11 @@
 
 struct lineedit_statics;
 
-#ifndef GCC_COMBINE
+#ifndef ORTHOX_BUSYBOX_LINEEDIT_PTR_HACK
+#define ORTHOX_BUSYBOX_LINEEDIT_PTR_HACK 0
+#endif
+
+#if !defined(GCC_COMBINE) || ORTHOX_BUSYBOX_LINEEDIT_PTR_HACK
 
 /* We cheat here. It is declared as const ptr in libbb.h,
  * but here we make it live in R/W memory */
diff --git a/shell/ash.c b/shell/ash.c
index d8f41327b..5cf6b9ae4 100644
--- a/shell/ash.c
+++ b/shell/ash.c
@@ -377,7 +377,13 @@ struct globals_misc {
 #endif
 	pid_t backgndpid;        /* pid of last background process */
 };
+/* Orthox BusyBox build uses a writable pointer here to avoid Clang/RISC-V
+ * miscompiling the cast-based initialization hack used by upstream ash. */
+#if ORTHOX_BUSYBOX_ASH_PTR_HACK
+extern struct globals_misc *ash_ptr_to_globals_misc;
+#else
 extern struct globals_misc *const ash_ptr_to_globals_misc;
+#endif
 #define G_misc (*ash_ptr_to_globals_misc)
 #define exitstatus        (G_misc.exitstatus )
 #define back_exitstatus   (G_misc.back_exitstatus )
@@ -1430,7 +1436,11 @@ struct globals_memstack {
 	size_t g_stacknleft; // = MINSIZE;
 	struct stack_block stackbase;
 };
+#if ORTHOX_BUSYBOX_ASH_PTR_HACK
+extern struct globals_memstack *ash_ptr_to_globals_memstack;
+#else
 extern struct globals_memstack *const ash_ptr_to_globals_memstack;
+#endif
 #define G_memstack (*ash_ptr_to_globals_memstack)
 #define g_stackp     (G_memstack.g_stackp    )
 #define g_stacknxt   (G_memstack.g_stacknxt  )
@@ -1982,7 +1992,11 @@ struct globals_var {
 	struct var *vartab[VTABSIZE];
 	struct var varinit[ARRAY_SIZE(varinit_data)];
 };
+#if ORTHOX_BUSYBOX_ASH_PTR_HACK
+extern struct globals_var *ash_ptr_to_globals_var;
+#else
 extern struct globals_var *const ash_ptr_to_globals_var;
+#endif
 #define G_var (*ash_ptr_to_globals_var)
 #define shellparam    (G_var.shellparam   )
 //#define redirlist     (G_var.redirlist    )
@@ -8796,10 +8810,17 @@ evaltree(union node *n, int flags)
 	return exitstatus;
 }
 
+#if ORTHOX_BUSYBOX_ASH_NO_NORETURN_ALIAS
+/* Clang は alias の __noreturn__ を alias 先 (evaltree 本体) に伝播させるため、
+ * 通常の evaltree 呼び出しの後続コードが到達不能として削除される。
+ * Orthox の Clang/RISC-V ビルドでは noreturn エイリアスを使わない。 */
+#define evaltreenr evaltree
+#else
 #if !defined(__alpha__) || (defined(__GNUC__) && __GNUC__ >= 3)
 static
 #endif
 int evaltreenr(union node *, int) __attribute__ ((alias("evaltree"),__noreturn__));
+#endif
 
 static int
 skiploop(void)
